#include <string.h>
#include <stdio.h>

char	*strnstr(const char *haystack, const char *needle, size_t len)
{
	if (*needle ='\0')
		return (*haystack);
	while(*s1 && *s2 && len--)
	{

	}

}

int main()
{
	char	*s1 = strdup("spasdp");
	char	*s2 = strdup("as");
	printf("%s\n", ft_memchr(s1, s2, 5));
	printf("%s\n", memchr(s1, s2, 5));
}